using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestManager : MonoBehaviour
{
    public TestMains testmain = new TestMains();
    public GameObject[] room = new GameObject[10];
  
    void Update()
    {
      
    }
    public void RoomSystem()
    {

        // room[testmain.Score] = new GameObject[testmain.Score];
        //for (int x = 1; x < testmain.Score; x++)
        //{
        //    Debug.Log("Масств" + room.Length);
        //    Debug.Log("тУТ" + room[x]);
        //    //room[x] =
        //    //    testmain.hERO[x];
           
        //}
    }
}
